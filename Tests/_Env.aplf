_Env←{
    ⍝ get value of environment variable or #-based variable (case-sensitive, prefixed with 'EWC_test_')
    ⍝ defaults taken from DEFAULTS ns. (using prefix 'EWC_')
     z←2 ⎕NQ #'GetEnvironment'('EWC_test_',⍵)
     def←{2=#.⎕NC'EWC_',⍵:#⍎'EWC_',⍵ ⋄ #.DEFAULTS⍎⍵}⍵
     0=≢z:def
     (⎕DR z)∊80 160:2⊃⎕VFI z
     z
 }