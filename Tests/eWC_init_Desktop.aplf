 r←eWC_init_Desktop
 r←''
 :Trap (~##.halt)/0
     eWC_init_loader 1⊃1 ⎕NPARTS ##.TESTSOURCE,'../'
     ROOT←'http://localhost:22344'
     BRWSR←'HtmlRenderer190'
     CMD_SRV_SUFFIX←' -cef --remote-debugging-port=8080'
     MODE←0   ⍝ jswc in desktop mode
     EWC_SHOWDEVTOOLS←1
 :Else
     r←⎕DMX.Message
 :EndTrap
